/*
Didn't do, b/c he used winforms

Using the given text file (primary.txt) display each line of the file that matches a search box on a form application.

Bonus:
Instead of returning the entire line, search the given text file (bonus.txt) and pull out all the phone numbers that match the pattern (123) 555-1212 (make sure there is a space on both sides)
*/

static void primary()
{

}

static void bonus()
{

}
